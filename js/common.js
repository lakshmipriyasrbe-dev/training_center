// Common JS functions for Training Center Management

function showAlert(message, type = 'info') {
    // Custom premium alert logic
    console.log(`[${type.toUpperCase()}] ${message}`);
}

$(document).ready(function() {
    // Global initializations
    console.log("Training Center Management System Initialized");
});
